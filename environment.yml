name: flowIDS

channels:
  - conda-forge

dependencies:
  - python
  - pip
  - setuptools
  - wheel
  - ipykernel
  # dependencies
  - polars
  - scikit-learn
  - pyarrow
  - matplotlib
  - pytorch
  - huggingface_hub
  # dev dependencies
  - tqdm
  - click
  - rich
